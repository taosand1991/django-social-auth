# Django Social Login without third party libraries

## `Procedure`

### cd into django_social
### `activate` virtual environment
### run `pip install -r requirements.txt`
### run `python manage.py runserver` to start the server
